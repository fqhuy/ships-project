/*
 * matrix_operations.cpp
 *
 *  Created on: Mar 17, 2011
 *      Author: fqhuy
 */

#include <ships/core/core.hpp>

namespace Sp{
template<class T> Matrix<T, T>* DeviceMatrixSorter<T>::Perform(const Matrix<T, T>* src, Matrix<T, T>* dest){

}

template<class T> std::vector<Matrix<T, T>* > DeviceMatrixSorter<T>::Perform(const std::vector<Matrix<T, T>* >& srcs, std::vector<Matrix<T, T>* > dests){

}

}
