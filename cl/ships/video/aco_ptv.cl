
__kernel void cluster(unsigned int size){
/*	int HALF = SIZE/2 + 1;
	int i = 0; 
	int j = get_global_id(0);

		for(int k=1;k<HALF;k++){
			if( j < SIZE ) {
				i++;
				j++;
				dis(i,j);				
			} else {
				i = HALF;
				j = SIZE - gid + i;
				dis1(i,j);
			} 
		}
		cout << endl;
*/
};
